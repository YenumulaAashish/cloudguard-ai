# Phase 2 validation and resumption audit

Executed locally on 2026-09-21. No AWS plan, deployment, bootstrap provisioning, IAM/OIDC configuration or GitHub push was performed. Local reports were generated by the actual offline runner; counts below are a dated observation, not permanent expected test totals.

## State found when resuming

`git status`, `git diff`, `git diff --staged` and `git remote -v` were inspected first. This checkout was on master with no commits, all project files untracked, no staged changes and no origin. Both diffs were empty because nothing had been committed or staged. No Git history or configuration was overwritten.

| Requirement at resumption | Observed state |
|---|---|
| Phase 1 modules, six annotations and original fixtures | Present and preserved |
| Machine exception contract and negative tests | Implemented; individual tests already executable |
| Offline/plan workflows, setup action and CODEOWNERS | Coded; hosted execution not started |
| Plan-only bootstrap role and trust policy | Coded and locally Terraform-valid; no AWS configuration |
| Plan exit handling, JSON export and Conftest path | Implemented; needed deeper process/failure tests and final integrated validation |
| Unknown-value/change-record Rego handling | Implemented with 34 tests at resumption, including original 24 |
| Evidence and Make targets | Implemented; final integrated evidence not yet verified |
| Phase 2 documentation | Incomplete; OIDC/evidence manuals absent and several docs still described Phase 1 |
| Interrupted files | Python/YAML syntax intact; no truncated source file found |

## Work completed after resuming

Completed the Phase 2 README, architecture/research mapping, setup/version documentation, OIDC/manual bootstrap guide, evidence guide and bootstrap README. Extended policies to block wholly unresolved final resource values. Added mocked process tests for exit 0/1/2 and failed JSON export, stale-evidence checks, nested suppression detection, malformed report handling, helper-command deployment detection, CI failure-masking checks and rejection of bootstrap exception annotations. Existing tests were retained.

Ran the exact portable Python entry point behind `make test` with AWS lint enabled. It completed successfully and generated reports/phase2/summary.json and summary.md. The fresh source scan has 30 passed, 0 failed, 6 skipped and no parsing errors across 18 resources. Counts differ from Phase 1 because security-group ingress is now represented inline; no new suppression was introduced.

| Validation | Result | Evidence |
|---|---|---|
| Terraform formatting (workload and bootstrap) | PASS | Both roots checked recursively |
| Backend-free initialization (both roots) | PASS | Pinned AWS provider 6.65.0; no AWS authentication |
| Terraform validation (both roots) | PASS | Actual provider-backed validation |
| TFLint with AWS plugin | PASS | Recursive workload lint |
| Python unit tests | PASS | 27 tests, including negative controls and simulated plan process failures |
| OPA/Rego | PASS | 35 tests, including all original 24 |
| Conftest fixtures | PASS | Secure fixture accepted; all five insecure fixtures rejected for expected reasons |
| Checkov source scan | PASS | 30 passed / 0 failed / 6 skipped / 0 parsing errors |
| Source-and-scan exception contract | PASS | Expected 6 / observed 6 / source 6 / missing 0 / unexpected 0; global suppression false |
| Destructive-CI guard | PASS | Executable scripts, Makefile and GitHub workflow/action files inspected |
| Python/Bash/YAML syntax and workflow boundaries | PASS | Syntax parsing, fork predicate, permissions, environment and explicit artifact paths checked |
| IAM action-name sanity | PASS | 29 named actions found in installed service-authorization metadata; not a live permission test |
| Integrated portable offline runner | PASS | `python scripts/run-offline.py` with TFLINT_AWS=1; process exit 0 |
| Evidence generation and sanitization | PASS | Actual runner output plus tests excluding sentinel secrets/messages and stale results |
| GNU `make test` invocation | NOT RUN | GNU Make unavailable on this native Windows host; exact Python target executed instead |
| Hosted GitHub offline CI | NOT RUN | No push or hosted run performed |
| AWS-side OIDC/bootstrap configuration | NOT RUN | Owner/admin configuration remains manual |
| Authenticated AWS plan and real-plan integration | NOT RUN | AWS authentication/setup unavailable locally; no live AWS plan attempted |

## Implemented versus externally verified

Real-plan policy evaluation is implemented using Terraform show JSON and the documented resource-change/unknown-value structures. Tests use deterministic synthetic data and mocked subprocesses; they are not an actual AWS plan. OIDC status is CODED (PASS), CONFIGURED (NOT RUN), VERIFIED (NOT RUN).

The six resource exceptions remain exactly as recorded in [security-exceptions.md](security-exceptions.md) and security/checkov-exceptions.json. Skipped checks are never counted as passed. Optional Checkov plan scanning is intentionally disabled because equivalent source-scoped skip semantics have not been verified.

For the first hosted authenticated run, complete [github-oidc.md](github-oidc.md) in order. Before Phase 3, review remote state/state access, deployment and drift experiment scope, and whether these educational sandbox exceptions remain appropriate. No such cloud changes are authorized by this implementation.

## Reproduce

```sh
# Install/configure the documented toolchain first.
TFLINT_AWS=1 python scripts/run-offline.py
python scripts/verify-checkov-exceptions.py
python scripts/verify-no-deployment-ci.py
python scripts/generate-evidence.py
# With GNU Make installed:
TFLINT_AWS=1 make test
```

The initial Phase 1 run recorded 35 passing Checkov checks, six skips and 24 OPA tests; this is historical context only. Evidence now records actual results, versions, source fingerprint, run ID and the absence of a Git commit instead of copying old counts.
